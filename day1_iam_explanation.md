What I did today:
Created a User: I made a personal account called Yusuf-Admin. I will use this for my daily work instead of the "Master Key" (Root account).

Locked the Door: I turned on MFA. Now, even if someone steals my password, they need my phone to get in.

Made a Special Key: I created a Role called S3-Reviewer. This is like a temporary badge that allows a server to read files but not delete them.

Key Concepts:
IAM: The security guard. It checks badges and decides who can enter which room.

Root Account: The Master Key. It stays in the safe. We don't use it for regular work.

Least Privilege: Only giving the keys to the rooms you actually need. No "unlimited" access for anyone.
